# IDE
.idea/
*.iml
.vscode/

# Build
build/
**/build/
.gradle/
.kotlin/
*.apk
*.aab
local.properties

# Signing (release keystore is a secret, kept out of git entirely — see
# .github/workflows/release.yml and the credentials handed to the user
# directly, not committed anywhere)
*.keystore
*.jks

# Claude
.claude/

# Vendor (tracked separately or too large)
vendor/

# OS
.DS_Store
Thumbs.db

# Screenshots pulled during testing
/tmp/tile_screenshot_*.png
